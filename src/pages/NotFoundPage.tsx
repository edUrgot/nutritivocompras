import { EmptyState } from "@/components/ui/EmptyState";

export function NotFoundPage() {
  return <EmptyState title="Página no encontrada" description="La ruta que intentas abrir no existe en esta versión de la app." />;
}
